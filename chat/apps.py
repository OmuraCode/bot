from django.apps import AppConfig
# from keras.models import load_model

class ChatConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'chat'

# loaded_model = load_model('bot_model.keras')
